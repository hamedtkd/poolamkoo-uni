# v0.5.2 — New Money dialog lint cleanup

## Fix

- رفع آخرین خطای `react-hooks/set-state-in-effect` در `use-new-money`.
- چرخهٔ عمر فرم «پول جدید دارم» دیگر با `setState` داخل Effect ریست نمی‌شود.
- محتوای دیالوگ فقط هنگام باز بودن mount می‌شود؛ بنابراین هر بار باز شدن، `step` و فرم‌ها به‌صورت طبیعی از state اولیه شروع می‌شوند.
- هیچ Rule از ESLint خاموش نشده و رفتار UX دیالوگ حفظ شده است.

## Quality target

این patch مخصوص رفع آخرین خطای گزارش‌شده در `npm run check` نسخه 0.5.1 است.
