# Experimental only — NOT required for normal Poolamkoo deployments.
# Background Push is paused by default to keep the public project zero-cost and quota-safe.
NEXT_PUBLIC_EXPERIMENTAL_BACKGROUND_PUSH=1

WEB_PUSH_VAPID_PUBLIC_KEY=
WEB_PUSH_VAPID_PRIVATE_KEY=
WEB_PUSH_VAPID_SUBJECT=mailto:you@example.com
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
CRON_SECRET=

# Required by the preserved experimental evaluator.
TINDEX_API_TOKEN=
