# v1.0.1 public launch readiness

تاریخ Candidate: 2026-08-30.

این Patch فقط quota/cache/backoff را harden می‌کند. IndexedDB schema 8 و مدل Local-first بدون تغییر باقی می‌مانند.

## Automated acceptance

| Gate | انتظار |
| --- | --- |
| TypeScript / ESLint / unit tests | Pass |
| Architecture / regression / performance checks | Pass |
| Production Next.js build | Pass |
| Production Browser Smoke | Pass |
| BrsApi cache guard | 180s |
| TSETMC Quote/Search/History cache | 120s / 600s / 3600s |
| Tindex fallback / legacy cache | 1800s / 3600s |
| 429/blocked runtime cooldown | Covered by unit tests |
| 30s same-target client request reuse | Covered by regression test |
| Core-only `/api/market` CDN response cache | 60s shared / target queries private |
| Version / schema / release docs | `1.0.1` / schema 8 / aligned |

## Manual pre-launch acceptance

- [ ] Vercel Project → Settings/Functions نشان دهد **Fluid Compute Enabled** است و deployment مورد استفاده بعد از فعال‌شدن آن ساخته شده است.
- [ ] Vercel Observability → Functions برای `/api/market` baseline Invocation و Active CPU قبل از معرفی ثبت شود.
- [ ] BrsApi key production معتبر باشد و refresh نرخ‌های پایه در Settings بدون 401/429 کار کند.
- [ ] Tindex برای کار عادی لازم نباشد؛ در صورت نبود Token، بورس جدید با TSETMC و نرخ پایه با BrsApi کار کند.
- [ ] چند refresh سریع بازار بیشتر از یک request client در پنجره 30 ثانیه تولید نکند و UI بدون خطا باقی بماند.
- [ ] شبیه‌سازی/مشاهده 429 یا blocked باعث diagnostic `guarded`/cooldown شود و Snapshot واقعی حفظ شود.
- [ ] 24 ساعت بعد از معرفی عمومی، Invocation، 429/blocked و BrsApi/Tindex usage دوباره بررسی شود.

## Release decision

- **Pass:** Gate خودکار سبز + Fluid Compute تأییدشده + Provider production healthy → آماده Tag/Release `v1.0.1`.
- **Block:** Fluid Compute خاموش، BrsApi 429 نزدیک/فعال، یا Provider retry burst قابل تکرار → Release متوقف و فقط همان blocker اصلاح شود.
