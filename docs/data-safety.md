# Data safety model

پولم‌کو Local-First است؛ این یعنی داده مالی اصلی داخل IndexedDB همان Origin نگه داشته می‌شود، نه روی سرور مرکزی.

## سه لایه حفاظت

1. **IndexedDB اصلی** برای کار روزمره.
2. **Recovery Snapshot محلی** برای برگشت از حذف یا Restore اشتباه. این Snapshot در همان Browser Storage است و در برابر پاک‌شدن Site Data یا خرابی دستگاه محافظت نمی‌کند.
3. **Backup فایل** برای نسخه مستقل از مرورگر. برای استفاده جدی، نسخه رمزنگاری‌شده را در فضای دیگری مثل دیسک دوم یا فضای ابری شخصی نگه دارید.

## یادآوری Backup

بعد از حداقل ۲۴ ساعت استفاده معنادار، اگر بکاپی ثبت نشده باشد اپ پیشنهاد بکاپ می‌دهد. بعد از بکاپ، اگر ۷ روز بگذرد دوباره یادآوری فعال می‌شود. کاربر می‌تواند آن را ۳ روز Snooze کند.

## Recovery history

حداکثر ۵ Snapshot جدید نگه داشته می‌شود. Snapshot خودکار حداکثر روزی یک بار ساخته می‌شود و عملیات تخریبی مهم نیز قبل از اجرا Recovery Point می‌سازند.

## Restore

قبل از جایگزینی داده با Backup فایل یا Recovery Snapshot، وضعیت فعلی محلی ذخیره می‌شود. Import فقط جدول‌های داده اصلی را جایگزین می‌کند و متادیتای دستگاه و Recovery history را پاک نمی‌کند.

## محدودیت صادقانه

Browser Storage بکاپ دائمی نیست. حتی اگر مرورگر Persistent Storage را تأیید کند، کاربر یا سیستم همچنان می‌تواند Site Data را پاک کند. تنها Backup خارج از همان Browser Profile یک نسخه مستقل محسوب می‌شود.

## Update و چند تب

از v0.20 اگر نسخه جدید Poolamkoo نیاز به بازکردن ساختار جدید IndexedDB داشته باشد، تب قدیمی اجازه ندارد بی‌صدا با اتصال قبلی به نوشتن ادامه دهد. رویدادهای `blocked` و `versionchange` به UI منتقل می‌شوند؛ تب قدیمی اتصال Dexie را می‌بندد و کاربر برای Reload امن راهنمایی می‌شود. Live Queryهای مالی نیز تا پایان Bootstrap دیتابیس شروع نمی‌شوند.

این مکانیزم Backup نیست و داده را جابه‌جا یا پاک نمی‌کند؛ فقط ریسک هم‌زمانی بین نسخه قدیمی و جدید اپ روی همان Origin را کاهش می‌دهد.

## بکاپ قابل‌راستی‌آزمایی در v0.21

فایل‌های بکاپ جدید از Envelope نسخه ۲ استفاده می‌کنند. این Envelope علاوه بر زمان خروجی، وضعیت رمزنگاری، نسخه برنامه و نسخه ساختار دیتابیس، یک SHA-256 digest از Metadata نسخه ۲ و Payload ذخیره می‌کند تا خرابی یا ناقص‌شدن فایل قبل از Restore تشخیص داده شود. برای بکاپ رمزدار، AES-GCM همچنان لایه احراز اصالت رمزنگاری‌شده است؛ SHA-256 اینجا برای تشخیص خرابی فایل است و جای امضای دیجیتال را نمی‌گیرد.

قبل از جایگزینی داده، پولم‌کو اکنون این مراحل را انجام می‌دهد:

1. Envelope و تاریخ خروجی را بررسی می‌کند.
2. اگر فایل رمزدار باشد آن را با رمز کاربر باز می‌کند.
3. digest فایل v2 را بررسی می‌کند.
4. نسخه Schema را با نسخه Local Database فعلی مقایسه می‌کند.
5. جدول‌های ضروری و Settings را اعتبارسنجی می‌کند.
6. تعداد پول‌های ورودی، صندوق‌ها، گردش‌های صندوق، دارایی‌ها و تراکنش‌ها را به‌صورت Preview نشان می‌دهد.
7. فقط بعد از تأیید کاربر یک Recovery Snapshot از وضعیت فعلی می‌سازد و Import را شروع می‌کند.

بکاپ‌های قدیمی v1 همچنان قابل خواندن‌اند و به‌عنوان Legacy شناخته می‌شوند. اما اگر فایل یا Snapshot با Schema جدیدتری از نسخه نصب‌شده ساخته شده باشد، Import قبل از هر Clear/Replace متوقف می‌شود و کاربر باید ابتدا خود برنامه را به‌روزرسانی کند.

Recovery Snapshotهای جدید نیز `schemaVersion` و `appVersion` دارند. Snapshotهای قدیمی بدون این Metadata حذف یا بی‌اعتبار نمی‌شوند و همچنان با Validation معمول به‌عنوان Legacy بررسی می‌شوند.

## خروجی گزارش در v0.29

خروجی Reports با Backup فرق دارد و برای انتقال کامل دیتابیس طراحی نشده است. دو سطح آگاهانه وجود دارد:

- **خلاصه اشتراک:** فقط درصدها و وضعیت تصمیمی را می‌سازد و مبلغ‌ها و نام دارایی‌ها را عمداً حذف می‌کند. این متن فقط پس از اقدام صریح کاربر به Clipboard یا Share Sheet سیستم‌عامل تحویل داده می‌شود.
- **CSV کامل:** فایل محلی شامل جزئیات همان گزارش، از جمله مبلغ‌ها و نام/ارزش دارایی‌هاست. این گزینه برای نگه‌داری/تحلیل شخصی است و تراکنش خام، Backup payload، Recovery Snapshot یا Metadata دستگاه را Export نمی‌کند.

Poolamkoo برای این قابلیت لینک اشتراک میزبانی‌شده، Upload خودکار یا دیتابیس گزارش سروری اضافه نمی‌کند. نام‌های متنی قبل از CSV در برابر Spreadsheet formula prefixهای خطرناک خنثی می‌شوند.


## مهاجرت Schema 7 در v0.34

این نسخه یک تغییر واقعی مدل داده دارد: دیده‌بان و هشدار بازار باید Provider را همراه `marketId` در هویت ماندگار خود حفظ کنند. به همین دلیل `LOCAL_DATABASE_SCHEMA_VERSION` از 6 به 7 می‌رود. Dexie نسخه 6 را در زنجیره نگه می‌دارد و هنگام Upgrade همان دیتابیس `poolyar-local` را in-place مهاجرت می‌دهد؛ دیتابیس جدیدی ساخته نمی‌شود.

رکوردهای قدیمی که `marketId` دارند ولی Provider در آن‌ها ثبت نشده، به Tindex نسبت داده می‌شوند؛ چون این رکوردها متعلق به نسل قبل از اتصال مستقیم TSETMC هستند. Backup/Recovery/Device Transfer قدیمی نیز قبل از نوشتن به schema 7 با همین قاعده نرمال می‌شود. Backup envelope v1/v2، رمزنگاری، SHA-256 integrity و pre-restore Recovery Snapshot تغییری نکرده‌اند. از v0.35، `npm run check:release` همین مسیر 6→7 را داخل Browser Profile موقت و با IndexedDB خام schema 6 به‌صورت خودکار اجرا می‌کند؛ این Fixture هرگز به Profile واقعی کاربر دسترسی ندارد.

## Schema 8 و دفتر گردش صندوق (v0.38)

Schema 8 جدول محلی `fundMovements` را اضافه می‌کند. `fund.currentToman` برای خواندن سریع باقی می‌ماند، اما تغییر موجودی از مسیر دفتر گردش انجام می‌شود و هر تغییر باید با replay تاریخی به موجودی غیرمنفی برسد. در Upgrade درجا، هر صندوق قدیمی با موجودی مثبت دقیقاً یک گردش `opening` با منبع `migration` می‌گیرد؛ صندوق صفر نیازی به ردیف ساختگی ندارد.

بکاپ/Recovery/Device Transfer قدیمی که `fundMovements` ندارند همچنان قابل استفاده‌اند: قبل از نوشتن، موجودی مثبت هر صندوق به همان opening migration نرمال می‌شود. اگر payload جدید دفتر گردش داشته باشد، اتصال هر گردش به صندوق، نوع/منبع، مبلغ مثبت، منفی‌نشدن تاریخچه و برابر بودن موجودی نهایی دفتر با `currentToman` پیش از هر Clear/Replace بررسی می‌شود. Backup envelope v1/v2، رمزنگاری و SHA-256 integrity تغییر نکرده‌اند. Release Browser Gate نیز از v0.38 مسیر واقعی schema 6→7→8 را در Profile موقت بررسی می‌کند.

## اصلاح امن پول ورودی در v0.39

ویرایش پول ورودی بعد از شروع اجرا اجازه بازنویسی تاریخچه را نمی‌دهد: مبلغ جدید باید حداقل به‌اندازه اجرای ثبت‌شده باشد و تاریخ ورودی پس از اولین اجرا قفل می‌شود. قبل از هر اصلاح معتبر یک Recovery Snapshot محلی ساخته می‌شود و سپس وضعیت جاری Income/Plan/Allocation/Transaction داخل همان مرز تراکنشی دوباره بررسی می‌شود. این قابلیت هیچ داده‌ای را از دستگاه خارج نمی‌کند و schema 8 یا فرمت Backup/Transfer را تغییر نمی‌دهد.


## سلامت و سازگاری داده در v0.40

Settings حالا یک Audit کاملاً محلی روی داده‌های اصلی اجرا می‌کند. این Audit رابطه پول ورودی با Allocation/Plan، اتصال تراکنش‌ها به دارایی و برنامه، تاریخچه غیرمنفی Ledger سرمایه‌گذاری و صندوق، وضعیت دارایی آرشیوشده با موجودی باز، و هویت‌های تکراری بازار را بررسی می‌کند. نتیجه فقط داخل همان Browser Profile نمایش داده می‌شود و هیچ مبلغ، نام دارایی یا رکورد مالی برای این بررسی به Backend یا Analytics فرستاده نمی‌شود.

ترمیم خودکار عمداً محدود است. فقط دو نوع خلاصه محاسباتی که منبع قطعی دارند بازسازی می‌شوند: `fund.currentToman` از یک Fund Ledger معتبر، و `PlanItem.executedToman` برای کارت سرمایه‌گذاری فقط وقتی خرید لینک‌شده وجود دارد. اجرای قدیمی بدون خرید لینک‌شده فقط گزارش می‌شود و خودکار صفر یا بازنویسی نمی‌شود. پیش از هر Write یک Recovery Snapshot ساخته می‌شود و داده جاری دوباره داخل یک Dexie transaction خوانده می‌شود. Orphan record، تاریخچه منفی، هدف گمشده، اجرای بیش از برنامه یا سایر حالت‌های مبهم خودکار حذف/حدس زده نمی‌شوند و برای بررسی کاربر باقی می‌مانند. Schema همچنان 8 است.

## تاریخچه یکپارچه در v0.41

صفحه Activity جدول جدید یا کپی دیگری از داده مالی ایجاد نمی‌کند. Timeline در لحظه از `incomes`، `fundMovements` و `transactions` موجود ساخته می‌شود و فقط برای نمایش به نام صندوق/دارایی‌های محلی متصل می‌شود. فیلتر تاریخ و جست‌وجو در مرورگر اجرا می‌شوند و هیچ رکورد، عبارت جست‌وجو یا خلاصه مالی برای ساخت Timeline به سرور ارسال نمی‌شود.

این صفحه موجودی نقد یا هزینه روزانه را استنتاج نمی‌کند. جمع‌های بالای صفحه فقط حجم رکوردهای موجود در بازه‌اند و ممکن است یک پول در مراحل مختلف جریان (ورودی، کنارگذاری صندوق، خرید دارایی) بیش از یک‌بار دیده شود؛ این رفتار عمدی است چون هدف Timeline نشان‌دادن رویدادهای ثبت‌شده است، نه ساختن حساب بانکی مصنوعی.
